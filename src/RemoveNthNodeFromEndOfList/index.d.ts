/**
 * Definition for singly-linked list.
 * function ListNode(val) {
 *     this.val = val;
 *     this.next = null;
 * }
 */
/**
 * @param {ListNode} head
 * @param {number} n
 * @return {ListNode}
 */
import { IListNode } from '../Interface';
declare const removeNthFromEnd: (head: IListNode, n: number) => IListNode;
export { removeNthFromEnd };
