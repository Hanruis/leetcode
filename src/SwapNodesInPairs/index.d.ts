/**
 * Definition for singly-linked list.
 * function ListNode(val) {
 *     this.val = val;
 *     this.next = null;
 * }
 */
/**
 * @param {ListNode} head
 * @return {ListNode}
 */
import { IListNode } from '../Interface';
declare const swapPairs: (head: IListNode) => IListNode;
export { swapPairs };
